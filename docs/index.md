# trapper_api_client

`trapper_api_client` es un cliente Python tipado para consumir la API de Trapper.

Incluye:

- componentes por recurso (`locations`, `deployments`, `resources`, etc.)
- utilidades de paginacion y exportacion
- descargas robustas con retry y paralelismo para media de clasificacion

